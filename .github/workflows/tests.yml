name: Builds and Tests
on: [pull_request]
jobs:
  checks:
    name: run
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@master

    - name: Test Docker build
      run: docker build .
